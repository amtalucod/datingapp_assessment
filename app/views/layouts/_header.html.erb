

<nav class="navbar navbar-expand-lg" style="background-color: #ff69b4;">
  <div class="container-fluid">
    <% if logged_in? && current_user.admin? %>
      <%= link_to 'Dating App', users_path, class: "navbar-brand" %>
    <% elsif logged_in? %>
      <%= link_to 'Dating App', swipes_path, class: "navbar-brand" %>
    <% else %>
      <%= link_to 'Dating App', login_path, class: "navbar-brand" %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent", style= "flex-grow: 0;">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      
        <% if logged_in? && current_user.admin? %>
          <li class="nav-item">
            <%= link_to 'Users Manager', users_path, class:"nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Matches Manages', likedpage_path, class:"nav-link" %>
          </li>
        <% end %>
        
        <% if logged_in? && !current_user.admin? %>  
          <li class="nav-item">
            <%= link_to 'Swipe Page', swipes_path, class:"nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Matches', matches_path, class:"nav-link" %>
          </li>
        <% end %>
        <% if logged_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Settings
            </a>
            <ul class="dropdown-menu">
              <li class="nav-item">
              <%= link_to 'Profile', current_user, class: "nav-link dropdown-item"%>
              </li>
              
              <li class="nav-item">
               <%= link_to 'Edit Profile', edit_user_path(current_user), class: "nav-link dropdown-item"%>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li class="nav-item">
               <%= link_to 'Sign Out', logout_path, method: :delete, class: "nav-link dropdown-item" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Log in', login_path, class:"nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign Up', signup_path, class:"nav-link" %>
          </li>
        <% end %>
      </ul>
   
    

    </div>
  </div>
</nav>

